s = {1, 5, 32, 54, 5, 5, "Harry"}
print(s, type(s))

s.add(566)
print(s, type(s))

s.remove(1)
print(s, type(s))

#s.pop()    will remove a random element
#s.clear()  will remove all elements

#sets are unordered it will print input in any order
#sets are unindexed (cannot access elements by index)
#no way to change items in sets
#cannot contain duplicate values
