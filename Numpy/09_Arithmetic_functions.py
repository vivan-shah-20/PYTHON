import numpy as np
a = np.array([1,2,3,4,5])
print("min :",np.min(a) , np.argmin(a))  #np.argmin gives the location of the number
print("max :", np.max(a) , np.argmax(a))
print("sqrt:", np.sqrt(a))